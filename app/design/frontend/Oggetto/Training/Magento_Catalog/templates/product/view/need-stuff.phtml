<p id="needStuff" data-color="red"><?= /* @escapeNotVerified */ __('You need this stuff') ?></p>

<script>
    require([
        'jquery',
        'colorize'
    ], function (
        $,
        colorize
    ) {
        var needStuff = '#needStuff',
            config = {
                color: $(needStuff).data('color')
            };

        colorize(config, needStuff);
    });
</script>
